# Recommended Test Games

> Moved from CLAUDE.md. A curated list of games used to validate UE5CEDumper across UE versions.

-----

| Game | UE Version | Notes |
|------|-----------|-------|
| EverSpace 2 | UE5.5 (PE: 505) | GNames via pointer-scan fallback. Stride 24, 1.16M objects. GWorld ✅ (build 1.0.0.27) |
| Titan Quest II | UE5.7 (PE: 507) | CasePreservingName + DynOff. Stride 16. 486,782 objects. GWorld ✅ via fallback ([GWorld]=0, UWorld found in GObjects) |
| OctoPath Traveler | UE4.22 (inferred) | 406060 objects. GNames via GNAM_CT3 ✅. GObjects via GOBJ_RE2 ✅ (Flat FFixedUObjectArray, validated by "Flat" preset). GWorld via GWLD_TQ_1 ✅. Codename "Kingship". Ghidra: GObjects RVA `0x29E5C20`, GNames RVA `0x29DCF08` (TNameEntryArray stride 0x4000). GOBJ_OT_1/OT_2 also added but untested (lower priority than RE2) |
| Final Fantasy VII Rebirth (FF7Re) | UE4.26 fork | Hash-prefixed FNameEntry (hdrOff=4, stride=4) + stride 24 — fully working. GWorld ✅ |
| Final Fantasy VII Remake Intergrade (FF7R) | UE4.18 fork | Flat FFixedUObjectArray ✅. UProperty fallback ✅. 315304 objects. GWorld ✅. Version: flat+UProperty → 418. Base GameInstance (9 fields, no BP subclass) |
| DQ I&II HD-2D Remake | UE5.05 (detected) | Stride 24, 128678 objects. GWorld ✅. SE HD-2D fork uses FFieldVariant=0x10 (UE5.0 layout) despite reporting UE505 — fixed by Step 6.5 inference (Name=0x28 from Next=0x20). BP_SantiagoGameInstance_C (64 fields) |
| DQ III HD-2D Remake | UE5.05 (detected) | 126022 objects. GWorld ✅. Same SE HD-2D fork layout as DQ I&II — FFieldVariant=0x10 inference fix applied |
| DQ XI S: Echoes of an Elusive Age | UE4.22 | 350251 objects. GWorld ✅. UField::Next=+0x38 (non-standard, probed). UProperty mode. Expanded UObject layout (+0x10 shift). BP_GameInstance_C (99 fields), BP_GameFlag_C (10 fields) |
| Tower of Mask | UE4.27 | Standard UE4 indie game — full pipeline confirmed working. Stride 24. GWorld ✅ (build 1.0.0.27) |
| Hogwarts Legacy | UE4.27 (PE: 427) | GNames via pointer-scan fallback. Stride 24, 379K objects. GWorld ✅ (build 1.0.0.27) |
| IDOLM@STER STARLIT SEASON | UE4.24 | Working. GWorld ✅ (build 1.0.0.27). CDO skip fix effective |
| Romancing SaGa 2 | UE4.27 | Working (build 1.0.0.27). GWorld ✅ |
| Star Wars Jedi: Fallen Order | UE4.21 | Working — 313 887 objects, **GWorld ✅** (`0x7FF7317EBAB8`) re-verified build 704 (2026-05-12) via CE injection. EA-launcher title: `version.dll` / `dinput8.dll` proxies do NOT load (EA app restricts the DLL search path); must inject via CE after the game is running. Install at `SwGame\Binaries\Win64\` holds TWO 58.4 MB exes side-by-side (`SwGame-Win64-Shipping.exe` + `starwarsjedifallenorder.exe`); CE sees the launcher exe as the running process. |
| Ghostwire: Tokyo | UE505 detected (possibly UE4) | Working (build 1.0.0.27). 254493 objects. GWorld ✅. UE version likely incorrect. RE-UE4SS has only AOB signatures, no version override |
| Lushfoil Photography Sim | UE5.6 (PE: 506) | NEW (build 1.0.0.40). All working. 58630 objects |
| Manor Lords | UE5.5 | NEW (build 1.0.0.40). All working |
| Satisfactory (v1.1.3.1) | UE5.3 (PE: 503) | Working — modular UE build with separate `FactoryGameSteam-CoreUObject-Win64-Shipping.dll` under `Engine\Binaries\Win64\`. `Macht::AOBScanAllModules` falls through to the CoreUObject DLL; the 15-game dump corpus (build 678 + 687) contains its 4 868 BPGCs cleanly. **GWorld ✅** re-verified build 704 (2026-05-12); the original "GWorld fails" note was stale (pre-`AOBScanAllModules`). Proxy deploy fixed build 691 — UI now finds the launcher at `Engine\Binaries\Win64\FactoryGameSteam-Win64-Shipping.exe` (real .exe is in `Engine\`, not `FactoryGame\`). |
| Satisfactory (v1.2.3.1) | UE5.6 (PE: 506) | NEW — re-verified after the **Satisfactory 1.2.3.1 update bumped the engine UE5.3 → 5.6** (build 1.0.0.1718, 2026-06-25, log dir `FactoryGameSteam-Win64-Shipping`). **Zero code changes vs the v1.1.3.1 / UE5.3 row above** — dynamic version detection + symbol-export globals + DynOff handled the bump transparently. Loaded via **`version.dll` proxy** in `Engine\Binaries\Win64\` (`FactoryGameSteam-Win64-Shipping.exe`, Steam, PID 4048). **UE version auto-detected as UE506** (memory scan, not hardcoded; CPN votes standard=20 / CPN=0). **All three globals resolved via symbol export — no AOB needed**: GObjects `?GUObjectArray@@3VFUObjectArray@@A` from `FactoryGameSteam-CoreUObject-Win64-Shipping.dll` (`0x7FFB9BC13620`), GNames via `?ToString@FName@@QEBAXAEAVFString@@@Z` from `…-Core-…dll` + SymbolCallFollow LEA (`0x7FFB9C49E8C0`), GWorld `?GWorld@@3VUWorldProxy@@A` from `…-Engine-…dll` (`0x7FFB905BDB68`). **217,602 objects** (Max 2.16M / 4 chunks), **FUObjectItem 24 B** (UE5-Extended, strict; stride-24 wins 200/200). DynOff: **CasePreservingName=no**, FProperty + **TaggedFFieldVariant (UE5.3+)**, all offsets standard UE5 — Outer +0x20; UStruct Super/Children/ChildProps/PropsSize/Script = 0x40/0x48/0x50/0x58/0x60; FField Class/Next/Name = 0x08/0x18/0x20; FProperty ElemSize/Flags/Offset = 0x34/0x38/0x44; FStructProp::Struct +0x70. FNamePool hdrOff=0 stride 2, BlockOffsetBits 16, Format A (shift 6). SparseDelegates via `SPARSE_ES2_1` AOB (multi-module). **GWorld walk ✅** — UWorld `World` 39 fields (propsSize 2736), PersistentLevel @+48, `FGGameInstance` 21 fields, WalkFunctions OK. Confirms the RE-UE4SS UE 5.6 deltas (GMalloc → `UE::Private` namespace, StaticConstructObject_Internal new signature) don't touch us — a read-only dumper uses neither. |
| Cat Island Petrichor Demo | UE5.6 | Full working. GWorld ✅ |
| Way of the Hunter 2 Demo | UE5.7 | Full working. GWorld ✅ |
| COMBAT PILOT: CARRIER QUALIFICATION Demo | UE5.5 | Full working. GWorld ✅ |
| The Artisan of Glimmith | UE4.27 (PE: 427, exe: Geri-Win64-Shipping) | Full working. Stride 24, 24K objects. GWorld ✅. **Build 648 cross-version PE-vtable verification target (2026-05-11)**: actual PE slot is `vtable+0x220` (old hardcoded `0x218` for UE 4.25-4.27 was off by 1 slot — enough to silently break invokes). Validator confirms 1260 hook fires / 1500ms. Four scenarios verified: Add_IntInt=7, Multiply_FloatFloat=12, `CharacterMovementComponent::GetMaxJumpHeight`=89.99 (instance method via game-thread dispatch), `PlayerCameraManager::GetCameraLocation` returns FVector struct. |
| Barn Finders | UE4.25 (PE: 425, exe: BarnFinders-Win64-Shipping) | User-submitted logs (build 560). Stride 24, 136,953 objects. UE5-Extended layout (strict). GWorld ✅. Standard FProperty mode. Publisher fallback (Atomic Jelly / Plug-In Digital — no thumbprint match). |
| Colossal | UE5.03 (PE: 503, exe: Colossal-Win64-Shipping, publisher: Atan) | User-submitted logs (build 560). Stride 24, 41,528 objects. UE5-Extended layout (strict). GWorld ✅. TaggedFFieldVariant (UE5.3+). FField::Next=+0x18, FField::Name=+0x20. Publisher Copyright is Epic default placeholder — no thumbprint match. |
| Extinction | UE4.15 (PE: 415, exe: Extinction.exe, dir: Blink/Binaries/Win64) | User-submitted logs (build 560). Stride 24, 230,732 objects. **Flat (non-chunked)** FFixedUObjectArray. GNames via TNameEntryArray (UE4Names=yes). UProperty mode (UE < 4.25). UField::Next=+0x28. **Lowest UE version verified end-to-end** — expands support below the previously documented 4.18+ floor. Patterns: GOBJ_RE2 (1.8s, 2 batches) / GNAM_CT3 (4.6s, 4 batches) / GWLD_G42_1 (3.3s, 3 batches). Publisher: Modus Games (no thumbprint). |
| MS Gundam SEED Battle Destiny Remastered (SBDR) | UE4.27 (PE: 427, exe: `SEED BATTLE DESTINY REMASTERED.exe`, dir: `Game_SBDR/Binaries/Win64`) | User-tested Steam title (build 1016). Stride 24, 57,423 objects (→72K mid-game). **All ✅** — GObjects (GOBJ_ES53_1, UE5-Extended layout strict, Max 2.16M / 33 chunks), GNames (GNAM_V8, FNamePool hdrOff=0 stride 2, UE4Names=no — a UE4.27 on the UE5-style FNamePool), **GWorld ✅** (GWLD_GH_1, `0x7FF758D77040`). FProperty mode (CPN=no, TagFFV=no, Outer=+0x20; FField Next=+0x20 Name=+0x28; FProp Offset=+0x4C StructProp=+0x78). `version.dll` proxy loads (real version.dll). ProcessEvent **vtable+0x220** — game-thread dispatch validated (15 646 hooks/1500 ms), invokes OK. Internal UClasses use a "Life" prefix (`LifeGameInstance`, 15 fields; `BP_LifeSaveData_C`). Bandai Namco (publisher=- — no thumbprint match). |
| The Adventures of Elliot: The Millennium Tales (冒險家艾略特的千年奇譚) | UE5.4 (runtime-reconciled — PE version stripped → publisher fallback 427, upgraded via tagged FFieldVariant→503 + CMC::GravityDirection→504, exe: `Elliot-Win64-Shipping.exe`, Steam folder `The Adventures of Elliot_The Millennium Tales/Elliot/Binaries/Win64`) | **The dxgi.dll proxy's motivating title** (build 1168+, 2026-06-15). **Re-verified on the FULL RELEASE (no longer the Prologue Demo), build 1363 (2026-06-19).** Stride 24, ~390K objects (389,292 at scan → 406,873 mid-game). UE5-Extended layout (strict; Max 2.16M / 33 chunks). **All ✅** — GObjects (GOBJ_ES53_1, `0x149BFB140`), GNames (GNAM_V8, `0x149B17600`, FNamePool hdrOff=0 stride 2, UE4Names=no — a UE4.27 on the UE5-style FNamePool like SBDR), **GWorld ✅** (GWLD_TQ_1, `0x149D8ADA0`). FProperty mode, TaggedFFieldVariant (UE5.3+), CPN=no, Outer=+0x20; FField Next=+0x18 Name=+0x20; FProp Offset=+0x44 StructProp=+0x70. Name sanity 10/10. **UE version: the stripped PE falls back to UE4.27, but runtime markers reconcile it upward to UE5.4** — tagged FFieldVariant → floor 5.3, then `CMC::GravityDirection` → 5.4 (self-correcting in `UE5_Init`; build ~1808, 2026-06-27). The UE5-style FNamePool + TaggedFFieldVariant were already UE5 tells; "4.27" was only ever the publisher-fallback guess. **ProcessEvent vtable+0x260 — game-thread dispatch validated (6 096 hooks / 1500 ms), invokes OK.** **The EXE imports NEITHER `version.dll` NOR `dinput8.dll` (only `dxgi` + `winmm`; D3D12 title)** → both existing proxies never load (confirmed by PE import-table analysis); the **`dxgi.dll` proxy DOES — LIVE-VERIFIED in-game** (`dxgi proxy: lazily forwarded 20/20 exports`, pipe + `trigger_scan` + `get_object_list` all OK). Internal UClasses use an `LS` prefix (`BPLSGameInstance_C`, `LSGameWork`, `CharacterAttributeSet`). Publisher SQUARE ENIX (no thumbprint match). |
| **Avowed** | UE5.3 (PE: 503, exe: `Avowed-Win64-Shipping.exe`, dir: `Alabama/Binaries/Win64`, Obsidian, Steam) | **Solved end-to-end via Ghidra ground-truth (builds 1230-1240, 2026-06-17). LIVE-VERIFIED.** **The FIRST verified game with a PACKED 20-byte `FUObjectItem` (stride `0x14`, not the standard 24)** — Obsidian drops the padding (Object@0 / Flags@8 / ClusterRoot@0xC / Serial@0x10); reading at stride 24 misaligns every object → garbage names + a string-bytes "count" (`0x00750061`="ua"=7,667,809). ~213K objects (95K at main menu). **No standard GObjects AOB matches — not even patternsleuth's** (confirmed by running its CLI on the exe). GObjects is a **static `FUObjectArray` at RVA `0xB5BE398`**, found via NEW patterns **`GOBJ_AV1`** (`MOV RDX,[ObjObjects.Objects]` in `AllocateUObjectIndex`) + **`GOBJ_AV2`** (the generic FUObjectItem chunk-index codegen, ~10+ sites, patch-resilient; the `lea*5;shl<<2` bakes in the 20-byte stride) — both resolve to GObjects+0x10 (adjustment `-0x10`). UE5-Extended layout (strict; Num 213K, Max 2.16M / 33 chunks). GNames `GNAM_V5` ✅. **GWorld AOB lands on a decoy → recovered via `instance_scan_recovery`** ✅ — scans `.data` for a slot pointing to a non-CDO UWorld, then picks the ACTIVE game world by non-null `OwningGameInstance` (skips World-Partition `_Generated_` transient cells, which have higher index but no GameInstance). Injects via the **`dxgi.dll` proxy**. Tracking doc: [docs/avowed-gobjects-fix.md](avowed-gobjects-fix.md). |
| Echoes of Aincrad Demo (艾恩葛朗特 迴盪新聲 Demo) | UE5.4 (PE undetected → default 504, exe: `EchoesofAincrad-Win64-Shipping.exe`, dir: `EchoesofAincrad/Binaries/Win64`, Bandai Namco) | User-tested Steam demo (build 1257, 2026-06-17). Stride 24, 238,216 objects. UE5-Extended "Default" layout (strict). **All ✅** — GObjects (GOBJ_V13, `0x147FD0B00`), GNames (GNAM_V5, FNamePool hdrOff=0 stride 2, UE4Names=no), **GWorld ✅** (GWLD_G42_3, `0x148630048`; live world `PL_Dungeon`, World 38 / Level 29 fields, PersistentLevel found). FProperty mode, TaggedFFieldVariant (UE5.3+), CPN=no, Outer=+0x20; FField Next=+0x18 Name=+0x20; FProp Offset=+0x44 StructProp=+0x70. Offsets validated against real `Guid`/`Vector` structs (10/10 name sanity); enum names ✅ (`ENetRole`, UEnum::Names @ +0x40); Value Search + Locate-in-GWorld (FindObjectGraphPath, 2 hops) confirmed. PE doesn't stamp a UE version (`Product=1.0`) → defaulted to 504, confirmed correct by the struct validation. Injects via the **`dxgi.dll` proxy** (forwarded 20/20 exports — same D3D12 case as Elliot). Internal codename **ROD** (`BP_RODGameInstance_C` super `RODGameInstance`, 35 fields). A Sword Art Online title; publisher Bandai Namco (no thumbprint match). |
| Solarpunk (太陽龐克) | UE5.7 (PE: 507, exe: `SolarpunkSteam-Win64-Shipping.exe`, rokaplay GmbH) | **The real-world repro that exposed + LIVE-CONFIRMED the UE5.7+ Object@+0x08 within-item detection (builds 1257-1259, 2026-06-17).** Stock UE5.7, **24-byte FUObjectItem with `Object`@+0x08** (`FlagsAndRefCount@0, Object@8, Serial@0x10`). 149,223 objects. **All ✅ after the fix** — the classic +0x00 pass is bad-dominated (`named=66 / bad=69`; a stride-16 scan over 24-byte items hits the real Object field only ~1/3 of the time) so it now falls through to the +0x08 pass → `FUObjectItem size=24, object-ptr offset=+0x08 — 200 named, 200 total, 0 bad` → name resolution **~100%** (`BP_MainPlayerController_C` 2 found, `nonNull==named`), sanity **10/10** (both were 45.9% / 4/10 *before* the fix). GObjects GOBJ_V13, GNames GNAM_SAT425_3 (after `GNAM_V5` hint miss → full scan), **GWorld ✅ via instance-scan recovery** (raw deref lands on a decoy `0x1C2D5` → recovered to the real UWorld). ProcessEvent vtable+0x260, dispatch validated (13 644 hooks/1500 ms). FProperty mode, TaggedFFieldVariant (UE5.3+); offset validation fell back to heuristic (Guid/Vector structs not found) but resolved correctly (ChildProps +0x50, FProp Offset +0x44). `version.dll` proxy. Closes the build-1064 "+0x08 still needs a real stock-5.7+ game" live-confirm. Publisher rokaplay (no thumbprint). |
| Stellar Blade (劍星) | UE4.26 fork (detected UE426, exe: `SB-Win64-Shipping.exe`, dir: `StellarBlade/SB/Binaries/Win64`, Shift Up, Steam) | User-tested (build 1928, 2026-07-05). Stride 24, 264,332 objects. **UE5-Extended layout (strict)** despite the UE4.26 fork (like SBDR / FF7 Rebirth — a UE4 on UE5-style structures; stride-24 wins 200/200, Object@+0x00). **All ✅** — GObjects (`0x146DD6230`), GNames (`0x146FFDC00`, FNamePool hdrOff=0 stride 2, Format A shift 6, UE4Names=no — a UE5-style pool on a UE4.26 fork), **GWorld ✅** (`0x146FFA8F8`). Name sanity 10/10. FProperty mode (UE 426 ≥ 425), CPN=no, TagFFV=no, Outer=+0x20; UStruct Super=+0x40 Children=+0x48 ChildProps=+0x50 PropsSize=+0x58; FField Next=+0x20 Name=+0x28; FProp Offset=+0x4C ElemSize=+0x3C StructProp=+0x78. `version.dll` proxy (loads the real version.dll from system32). **Fly (Dunste) verified** — view-relative velocity-drive flight works (MOVE_Flying held, velocity sticks). Publisher Shift Up (no thumbprint match). |
| Persona 3 Reload (女神異聞錄３ Reload) | UE4.27 fork (PE VERSIONINFO stripped `Product=1.0` → tool defaults to **504**, detected=no; **ProcessEvent slot vtable+0x220 = the UE4.25-4.27 slot** like SBDR/Geri; UE5-Extended layout + TaggedFFieldVariant. exe `P3R.exe`, dir `P3R/P3R/Binaries/Win64`, ATLUS/SEGA, Steam) | User-tested (build 1948, 2026-07-06, log dir `P3R`). Stride 24 (`Object`@+0x00; 200/200 named, 0 bad — stride-16 bad=69, stride-20 bad=104), 65,165 objects at scan (Max 2.16M / 33 chunks). **UE5-Extended layout (strict)** + **TaggedFFieldVariant (UE5.3+)**, FProperty mode, CPN=no — a UE4.27 title on UE5-style structures (like SBDR / Stellar Blade). **All globals ✅** — GObjects (GOBJ_ES53_1, 129 matches, `0x1457B4D10`), GNames (GNAM_V8 CallFollow, `0x1457789C0`, FNamePool hdrOff=0 stride 2, Format A shift 6, BlockOffsetBits 16, UE4Names=no, FName[0]='None'), **GWorld ✅ directly** (GWLD_GH_1 unique, `0x1458F8870` — no decoy/recovery), SparseDelegates (SPARSE_ES2_1, `0x145422370`). Offsets validated against real `Guid`/`Vector` structs: Outer=+0x20; UStruct Super/Children/ChildProps/PropsSize/Script = 0x40/0x48/0x50/0x58/0x60; FField Class/Next/Name = 0x08/0x20/0x28; FProp ElementSize/Offset = 0x3C/0x4C. **GWorld walk ✅** — World 35 fields (size 1944), GameInstance `GlobalWork` (super GameInstance, 43 fields, size 199216), PlayerController `ItfController` (super `KernelInput`, 133 fields), LocalPlayer 9 fields; WalkFunctions 32. **⚠ Foreground-pause quirk (`t.IdleWhenNotForeground=1`):** the game idles its game thread whenever it is NOT the foreground window, so the ProcessEvent hook-fire validator failed (**0 fires / 1500 ms**) and invoke/POV getters time out → POV falls back to the raw cached-POV read while backgrounded. The +0x220 slot is likely correct (pattern-matched, version-independent) — it just couldn't self-validate with the thread asleep. **Fix — Teleport tab → Keep Foreground (Grausam), LIVE-VERIFIED build 1955 (2026-07-06):** P3R's pause is **WM_ACTIVATEAPP-message-driven, not GetForegroundWindow polling** (the config `t.IdleWhenNotForeground 0` AND a GetForegroundWindow-only hook both had no effect). Grausam now hooks GetForegroundWindow **and** subclasses the game window's WndProc, rewriting the deactivation messages to "active" (WM_ACTIVATEAPP/WM_ACTIVATE/WM_NCACTIVATE→active, swallow WM_KILLFOCUS). Log confirms it subclassed the real 3840×2160 game window; invoke/POV ops (POV/Teleport/Console/Interesting-Functions) then work while backgrounded. `version.dll` proxy (loads the real system32 version.dll). PE hash `69CE343916376000`. Publisher ATLUS/SEGA (`(C)ATLUS. (C)SEGA.`, no thumbprint match). |
| Pionero Capital Demo | UE5.7 (PE: 507, exe: `PioneroCapital-Win64-Shipping.exe`, dir: `PioneroCapital/Binaries/Win64`, Pionero Games, Steam) | User-tested Steam demo (build 1.0.0.2202, 2026-07-15, log dir `PioneroCapital-Win64-Shipping`). **The second stock-UE5.7 `Object`@+0x08 title after Solarpunk** — a 24-byte `FUObjectItem` with the object pointer at +0x08 (`item_layout_mode=unpacked57`; `FlagsAndRefCount@0, Object@8, Serial@0x10`), 117,663 objects (Max 2.16M / 2 live chunks / 33 max). The classic +0x00 pass is bad-dominated (`named=66 / bad=69` — a stride-16 read over 24-byte items) so `Aura::DetectItemSize` falls through to the +0x08 pass → `FUObjectItem size=24, object-ptr offset=+0x08 (UE5.7+ reordered item) — 200 named, 200 total, 0 bad` → name sanity **10/10**. **All ✅** — GObjects (GOBJ_ES53_1, 89 matches, UE5-Extended layout strict, `0x7FF6F65689D0`), GNames (GNAM_V5, 11 matches, `0x7FF6F649A940`, FNamePool hdrOff=0 stride 2, Format A shift 6, BlockOffsetBits 16, UE4Names=no, FName[0]='None'), **GWorld ✅ directly** (GWLD_TQ_1 unique match, `0x7FF6F66F3780` — **no decoy / instance-scan recovery**, unlike Solarpunk), SparseDelegates (SPARSE_ES2_1, `0x7FF6F6296FF0`). FProperty mode, **TaggedFFieldVariant (UE5.3+)**, CPN=no (votes standard=20 / CPN=0), Outer=+0x20; UStruct Super/Children/ChildProps/PropsSize/Script = 0x40/0x48/0x50/0x58/0x60; FField Class/Next/Name = 0x08/0x18/0x20; FProp ElemSize/Flags/Offset = 0x34/0x38/0x44; FStructProp::Struct +0x70. Offsets validated against real `Guid`/`Vector` structs; enum names ✅ (`ENetRole`, UEnum::Names @ +0x40, UE5.6+ FNameData). FArrayProperty::Inner self-corrected +0x70 → +0x78. **GWorld walk ✅** — live world `NewPioneroIsland` (World, 42 fields, propsSize 2712), PersistentLevel @+48 (Level, 29 fields), GameInstance `JoebillGameInstance` (super GameInstance, 6 fields, size 456), LocalPlayer 9 fields, WorldSettings 143 fields, GameStateBase 88 fields; WalkFunctions OK. Value Search (Int32/Float/Double) + a NumericNoByte snapshot exercised clean (0 warnings/errors across all 5 log files). Loads via the **`dxgi.dll` proxy** (`load_mode=proxy:dxgi.dll`, forwarded 20/20 exports to the real System32 dxgi — a D3D12 title like Elliot / Echoes of Aincrad / Avowed; **the first stock-UE5.7 title verified end-to-end through the dxgi proxy**). Internal UClasses use a **`Joebill`** prefix (`BP_JoebillGameMode_C` 99 fields, `BP_JoebillPlayer_C` : `JoebillPlayer` 184 fields, `BP_MH_JoebillCharacter_C` 252 fields) — a city-builder / tycoon sim (`CitygenPrimitiveSpawnSettings`, `BP_TrafficLightsController_C`, `BP_PedTraffic_C`, `CC_Customizable_Character_C`). PE hash `C200F9770A5F1000`. Publisher Pionero Games (Copyright is the Epic default placeholder `Fill out your copyright notice…` — no thumbprint match). |

-----

### MindsEye — working on game v7.3.1 (GObjects ✅ / GNames ✅ de-obfuscated; game-specific symbols renamed at build time)

| Game | UE Version | Notes |
|------|-----------|-------|
| **MindsEye** | UE5.4.4 licensee fork (PE: 504, exe: `MindsEye-Win64-Shipping.exe`, dir: `MindsEye/Binaries/Win64`, Build A Rocket Boy, Steam; `version.dll` proxy) | **Solved end-to-end (builds 2220 + 2238), LIVE-VERIFIED on GAME VERSION 7.3.1 ONLY (PE hash `0863E3B90C993000`) — every RVA/offset below is build-specific; full re-derivation playbook in [mindseye-fork-notes.md](mindseye-fork-notes.md).** `GUObjectArray` RVA `0x0BB139B0` via new **`MindsEye-Extended`** preset (MaxElements `+0x10`, NumChunks `+0x14`, MaxChunks `+0x20`, NumElements `+0x24`, Objects `+0x28`) plus a **preset-bound `itemHint`**: `FUObjectItem` is **32 bytes with `UObject*` at `+0x10`** (65536 elem/chunk). 530,638 objects, Max 2,162,688 / 9 live chunks / 33 max, `200 total / 0 bad`. The item layout MUST stay preset-bound — 32/`+0x10` aliases perfectly with stride 16 and would steal TQ2 / Octopath if added to the shared sweep. GWorld ✅ (`GWLD_ES2_6` unique, `0x7FF60EE239B8`), SparseDelegates ✅ (`SPARSE_ES2_1`). Binary is **unpacked, no Denuvo/EAC/BattlEye**; `.rdata` keeps the `__FILE__` anchors (`J:\work\e18f6e32b612e2cd\Engine\Source\Runtime\...`), which is how the layout was recovered offline (capstone + `.pdata`, no Ghidra). ~206 MB exe / 145 MB `.text` from statically-linked Wwise 2023.1 + NVIDIA NGX + MetaHuman + the BARB `Everywhere`/`Arcadia` platform. **GNames ✅ — pool RVA `0x0BA306C0`, payload de-obfuscated (build 2238, experimental-gated).** Entry = stock `u16 header` at `+0x00` (`len = header>>6`), a **non-stock `u16 tag` at `+0x02`**, then XOR'd chars at **`+0x04`** (stock: `+0x02`), 2-byte aligned. Key is **per TAG** (an early per-block hypothesis was wrong), looked up in the fork's own open-hash map: tag `0x0001`→`0x09`, `0x0002`/`0x0082`→`0x5B`, `0x0003`→`0x1D`, `0x0016`→`0xA3`, `0x0036`→`0xE3`, `0x0061`→`0xC9`. Block 0 decodes to the canonical `None`, `ByteProperty`, `IntProperty`, … EName list. Decrypt routine RVA `0x0178B440` (ANSI) / `0x0178B540` (wide); `KeyDerive` RVA `0x0178CF50` is a locked hash-map probe (`bucket = tag & (capacity-1)`) into a runtime table at ctx RVA `0x0BA47700`. We **read that table directly and never call the routine** (its lock is taken before the probe, so a fault inside it would leave `FName::ToString` wedged forever). ctx is located statically: `Sig::AOB_NAMEDECRYPT_ME1` -> `E8 rel32` at match+`0x2F` -> getter -> first rip-relative `48 8D 05`. Live: `decrypt fn=0x7FF60492B440 getter=0x7FF604931050 keyTable=0x7FF60EBE7700`; name sanity **10/10**. **Game-specific symbols are UNRECOVERABLE by design** — MindsEye renamed its own non-engine property/class names to generated 16-char all-lowercase identifiers at build time (`wcxugjojsqaqvers`, ...); those strings sit verbatim in `.rdata` and the exe holds 21,635 distinct such tokens, so the originals exist nowhere. Engine symbols read normally. The pak/IoStore AES (CUE4Parse `MindsEyeAes.cs`) is real but irrelevant to in-process scanning. PE hash `0863E3B90C993000`. |

-----

## GWorld Status Summary

**Working (33/33):** TQ2, EverSpace 2, Hogwarts Legacy, IDOLM@STER, Romancing SaGa 2, Tower of Mask, Ghostwire: Tokyo, Cat Island Petrichor Demo, Way of the Hunter 2 Demo, COMBAT PILOT Demo, OctoPath Traveler, FF7R, FF7Re, DQ I&II, DQ III, DQ XI S, Lushfoil Photography Sim, Manor Lords, The Artisan of Glimmith, Barn Finders, Colossal, Extinction, MS Gundam SEED Battle Destiny Remastered, The Adventures of Elliot: The Millennium Tales, Avowed (instance_scan_recovery), Echoes of Aincrad Demo, Star Wars Jedi: Fallen Order (CE manual inject), Satisfactory (modular CoreUObject DLL — v1.1.3.1/UE5.3 + v1.2.3.1/UE5.6), Solarpunk (instance-scan recovery), Stellar Blade, Persona 3 Reload (GWLD_GH_1 direct), Pionero Capital Demo (GWLD_TQ_1 direct, stock UE5.7 Object@+0x08, dxgi proxy), MindsEye (UE5.4.4 licensee fork, **game v7.3.1 only** — see [mindseye-fork-notes.md](mindseye-fork-notes.md))

**Failing (GWorld not found or untested):** none — all 32 tested games resolve GWorld. Star Wars Jedi + Satisfactory were re-verified working at build 704 (2026-05-12); the earlier "failing" note was stale (pre-`AOBScanAllModules` / pre-CE-inject). Satisfactory was re-verified again on its **1.2.3.1 / UE5.6** build (build 1.0.0.1718, 2026-06-25) — symbol-export globals + GWorld walk all clean with zero code changes.

## Naming Convention

- **FF7Re** = Final Fantasy VII Rebirth (UE4.26 Square Enix fork)
- **FF7R** = Final Fantasy VII Remake Intergrade (UE4.18 Square Enix fork)
- **Geri** = The Artisan of Glimmith (UE4.27, executable `Geri-Win64-Shipping.exe` — log folder uses this name)
- **Blink** = Extinction (UE4.15, executable `Extinction.exe`, parent dir `Blink/Binaries/Win64/`)
- **SBDR** = MOBILE SUIT GUNDAM SEED Battle Destiny Remastered (UE4.27, Steam; executable `SEED BATTLE DESTINY REMASTERED.exe`, project dir `Game_SBDR/Binaries/Win64/`; internal UClasses use a `Life` prefix — `LifeGameInstance`, `BP_LifeSaveData_C`, etc.)
- **Elliot** = The Adventures of Elliot: The Millennium Tales / 冒險家艾略特的千年奇譚 (UE5.4 — its stripped PE detects as UE4.27 via publisher fallback, runtime-reconciled upward to UE5.4 via tagged FFieldVariant + CMC::GravityDirection markers; SQUARE ENIX, Steam; executable `Elliot-Win64-Shipping.exe`, Steam folder `The Adventures of Elliot_The Millennium Tales/Elliot/Binaries/Win64/`). Now the **full release** (re-verified build 1363, 2026-06-19; originally tested as the Prologue Demo). The title that motivated the **`dxgi.dll` proxy** (3rd proxy type) — its EXE imports neither `version.dll` nor `dinput8.dll`, only `dxgi`/`winmm`. Internal UClasses use an `LS` prefix.
- **Avowed** = Avowed (UE5.3, Obsidian, Steam; executable `Avowed-Win64-Shipping.exe`, project dir `Alabama/Binaries/Win64/`). The title that proved **packed 20-byte `FUObjectItem`** support and the **static-struct GObjects AOBs** (`GOBJ_AV1`/`GOBJ_AV2`) for games where no standard/patternsleuth pattern matches. Injects via `dxgi.dll` proxy.
- **Echoes of Aincrad** = Echoes of Aincrad Demo / 艾恩葛朗特 迴盪新聲 Demo (UE5.4, Bandai Namco, Steam; executable `EchoesofAincrad-Win64-Shipping.exe`, Steam folder `Echoes of Aincrad Demo/EchoesofAincrad/Binaries/Win64/`; internal UClasses use a `ROD` prefix — `RODGameInstance`, `BP_RODGameInstance_C`). A Sword Art Online title. PE stamps no UE version (`Product=1.0`) → defaults to 504, confirmed UE5.3+/5.4 by struct validation. Injects via the `dxgi.dll` proxy (3rd proxy type, like Elliot — D3D12 EXE imports neither `version.dll` nor `dinput8.dll`).
- **P3R** = Persona 3 Reload / 女神異聞錄３ Reload (UE4.27 fork, ATLUS/SEGA, Steam; executable `P3R.exe`, project dir `P3R/P3R/Binaries/Win64/`; internal UClasses: GameInstance `GlobalWork`, PlayerController `ItfController` : `KernelInput`). PE version stripped (`Product=1.0`) → tool defaults to 504; UE5-Extended + TaggedFFieldVariant layout with a UE4.25-4.27 ProcessEvent slot (**vtable+0x220**, like SBDR/Geri). All globals + GWorld walk ✅. Notable for the **foreground-pause quirk** (`t.IdleWhenNotForeground=1`): the game thread idles when the window is not focused, so invoke/POV getters time out (ProcessEvent validator saw 0 fires) while backgrounded — keep the game focused or set `t.IdleWhenNotForeground 0`. `version.dll` proxy.
- **Solarpunk** = Solarpunk / 太陽龐克 (stock UE5.7, rokaplay GmbH, Steam; executable `SolarpunkSteam-Win64-Shipping.exe`). The real-world repro that exposed + live-confirmed the **UE5.7+ `Object`@+0x08** within-item layout (24-byte item): its classic +0x00 pass is bad-dominated (`named ≈ bad`, a mis-strided read), so `Aura::DetectItemSize` now falls through to the +0x08 pass (the `bestNamed > bestBad` quality gate, build 1257). `version.dll` proxy.
- **Pionero Capital Demo** = Pionero Capital Demo (stock UE5.7, Pionero Games, Steam; executable `PioneroCapital-Win64-Shipping.exe`, project dir `PioneroCapital/Binaries/Win64/`; internal UClasses use a `Joebill` prefix — `JoebillGameInstance`, `BP_JoebillGameMode_C`, `BP_JoebillPlayer_C`; a city-builder / tycoon sim). No Chinese title — English only. The **second stock-UE5.7 `Object`@+0x08 title after Solarpunk**, re-confirming the +0x08 fall-through on a different game, but with two firsts: **GWorld resolves directly** (GWLD_TQ_1 unique — no decoy / instance-scan recovery that Solarpunk needed) and it is the **first stock-UE5.7 title verified through the `dxgi.dll` proxy** (D3D12 EXE, like Elliot / Echoes of Aincrad / Avowed). Build 1.0.0.2202, 2026-07-15.
